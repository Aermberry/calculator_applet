# 2026-09-09 工作日志

- 完成计算器小程序架构设计（专家：小程达/微信小程序开发）。
- 选型结论：原生小程序 + TDesign + 微信云开发；主包 <500KB 只放 tab 核心页，工具放 packageTool 分包；增长钩子（分享卡片、搜索 SEO、收藏、静默登录、订阅消息）内建于架构。
- 产出文档：docs/architecture.md；假设定位为"基础计算+单位换算+垂直工具箱"，待用户确认。
- 与用户深入讨论 services/core/infrastructure 分层（含通用架构理论），用户确认采用六边形思路：infrastructure 唯一触碰 wx.*，services 做业务编排。已更新 docs/architecture.md（新增 infrastructure/ 目录与"分层规约"章节），并写入 MEMORY.md 项目约定。
- 用户提供资料库参考文档《Flutter DDD 架构分析》（space 节点：architecture.svg drive + overview.md doc，公司 Flutter DDD 定制：ui→application→domain←persistence，COLA CQE + 防腐层）。据此重写 docs/architecture.md 为 DDD 版并新增 docs/architecture-ddd.svg 依赖图；参考原件已存 docs/reference-architecture.svg。
- 按 DDD 分层**落地代码骨架**：domain(cqe/entity/service/facade/repository/types/exceptions + event 预留) → application(calc/history/unit/share 用例 + errors + di 组合根) → infrastructure(wx 防腐层 6 文件) + persistence(本地仓储实现 + serializer) → pages/components/packageTool 微信 UI。配套 test/run.test.js（20 项 Node 单测全绿）与 test/check_build.js（全模块/JSON 加载零失败）。领域层 Decimal 解决 0.1+0.2 精度问题。
